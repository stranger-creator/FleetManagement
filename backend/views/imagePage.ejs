
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Billing</title>
    <style>
        /* Apply styles from Complaint.css */
        .complaint-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ffa500;
            border-radius: 10px;
            background-color: #ffebcc;
            box-shadow: 0 0 20px rgba(255, 165, 0, 0.5);
            margin-top: 20px;
        }

        .complaint-container h1 {
            text-align: center;
            font-size: 24px;
            font-family: Arial, sans-serif; /* Change the font */
        }

        .complaint-container form {
            display: flex;
            flex-direction: column;
        }

        .complaint-container label {
            font-weight: bold;
            margin-bottom: 10px;
            color: #ffa500;
        }

        .complaint-container input[type="text"],
        .complaint-container textarea {
            padding: 15px;
            margin-bottom: 15px;
            border: 2px solid #ffa500;
            border-radius: 5px;
            background-color: #fff;
        }

        .complaint-container textarea {
            resize: vertical;
        }

        .complaint-container button[type="submit"] {
            background-color: #ffa500;
            color: #fff;
            padding: 15px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            font-size: 18px;
            margin-top: 10px;
        }

        .complaint-container button[type="submit"]:hover {
            background-color: #ff8000;
        }

        /* Apply styles specific to your existing HTML structure */
        .image-container {
            display: flex;
            margin: 20px 0;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin: 20px 0;
        }

        .image-preview {
            max-width: 150px;
            max-height: 150px;
            margin-right: 20px;
        }

        .image-info {
            flex: 1;
        }

        h5, p {
            margin: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #ffffff; /* Change the background color */
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            padding: 20px 0;
            font-family: Arial, sans-serif; /* Change the font */
        }

        hr {
            border: 1px solid #ffffff; /* Change the border color */
        }

        form {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input[type="text"],
        textarea,
        input[type="file"] {
            width: 90%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button[type="submit"] {
            background-color: #FF5722;
            color: #fff;
            border: none;
            border-radius: 4px;
            padding: 10px 20px;
            cursor: pointer;
        }

        button[type="submit"]:hover {
            background-color: #FF5722;
        }

        h5, p {
            font-family: Arial, sans-serif; /* Change the font */
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto;
        }
        .action-button {
    background-color: #ff6600;
    color: white;
    border: none;
    padding: 10px 20px;
    font-size: 1rem;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }
  
    </style>
</head>
<body>
   
    <div class="complaint-container " > <!-- Apply the .complaint-container class to your form -->
        <h1>Cost Page</h1>
        <form action="/" method="POST" enctype="multipart/form-data">
            <div>
                <label for="name" style="width: 150px;">Email</label>
                <input
                    type="text"
                    id="name"
                    placeholder="Email"
                    name="name"
                    required
                />
            </div>
            <div>
                <label for="desc">Billing Details</label>
                <textarea
                    id="desc"
                    name="desc"
                    rows="2"
                    placeholder="Description"
                    required
                ></textarea>
            </div>
            <div>
                <label for="cost" style="width: 150px;">Cost</label>
                <input
                    type="number"
                    id="cost"
                    name="cost"
                    placeholder="Cost"
                    required
                />
            </div>
            <br>
            <div>
                <label for="image">Upload Image Proof/invoice</label>
                <input type="file" id="image" name="image" required />
            </div>
            <div>
                <button  type="submit" className="action-button ">Submit</button>
            </div>
        </form>
    </div>

    <hr />

    <h1>Uploaded Images</h1>
    <div>
        <% items.forEach(function(image) { %>
        <div class="image-container">
            <div>
                <div class="image-info">
                    <h5>Driver Name</h5>
                    <h5><%= image.name %></h5>
                    <h5>Bill details</h5>
                    <p><%= image.desc %></p>
                </div>
                <img
                    src="data:image/<%= image.img.contentType %>;base64, <%= image.img.data.toString('base64') %>"
                    class="image-preview"
                />
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>
